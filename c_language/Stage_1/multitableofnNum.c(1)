#include<stdio.h>
{
    int n,i,j,ans;
    void main()
    {
        printf("Enter the no of n");
        scanf("%d",&n);

        for(j=1;J<n;j++)
        {
            for(i=1;i<=10;i++)
            {
                ans=n*i;
            }
        }
        printf("%d",ans);
    }
}